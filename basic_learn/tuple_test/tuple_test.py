my_t = (123, "adb", "gh")
print(my_t)
# tuple same as list but can not be changed

#attention
my_t1 = (6)
print(type(my_t1)) # that be int not tuple
my_t2 = (6,)
print(type(my_t2)) # that be  tuple, 
# we can not define a empty tuple since it cant be modified so cacent tuple is meaningless
